server {
    listen 80;
    server_name _;
    root /usr/share/nginx/html;
    index index.html;

    location / {
        try_files $uri $uri.html $uri/ $uri/index.html /index.html;
    }

    # No cache for HTML
    location ~* \.html$ {
        expires -1;
    }
}
